services:
  anonymizer:
    build: .
    image: anonymizer:latest
    container_name: anonymizer
    network_mode: bridge
    ports:
      - "8501:8501"
    restart: unless-stopped
    volumes:
      - anonymizer-data:/home/appuser/Anonymizer

volumes:
  anonymizer-data:
